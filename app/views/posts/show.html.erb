<h1><%= @post.title %></h1>
<p><%= @post.content %></p>

<ul>
<%= link_to 'Edit', edit_post_path(@post) %> |
<li><%= link_to "Destroy", post_path(@post), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                    
                  } %></li>
</ul>